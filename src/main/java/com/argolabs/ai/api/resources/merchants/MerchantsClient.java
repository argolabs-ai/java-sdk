/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.argolabs.ai.api.resources.merchants;

import com.argolabs.ai.api.core.ArgoLabsApiException;
import com.argolabs.ai.api.core.ArgoLabsException;
import com.argolabs.ai.api.core.ClientOptions;
import com.argolabs.ai.api.core.ObjectMappers;
import com.argolabs.ai.api.core.RequestOptions;
import com.argolabs.ai.api.errors.UnprocessableEntityError;
import com.argolabs.ai.api.resources.merchants.requests.ReadMerchantsV1MerchantsGetRequest;
import com.argolabs.ai.api.types.HttpValidationError;
import com.argolabs.ai.api.types.MerchantResponse;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import java.io.IOException;
import java.util.List;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class MerchantsClient {
    protected final ClientOptions clientOptions;

    public MerchantsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public List<MerchantResponse> readMerchants() {
        return readMerchants(ReadMerchantsV1MerchantsGetRequest.builder().build());
    }

    public List<MerchantResponse> readMerchants(ReadMerchantsV1MerchantsGetRequest request) {
        return readMerchants(request, null);
    }

    public List<MerchantResponse> readMerchants(
            ReadMerchantsV1MerchantsGetRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v1/merchants");
        if (request.getSkip().isPresent()) {
            httpUrl.addQueryParameter("skip", request.getSkip().get().toString());
        }
        if (request.getLimit().isPresent()) {
            httpUrl.addQueryParameter("limit", request.getLimit().get().toString());
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return ObjectMappers.JSON_MAPPER.readValue(
                        responseBody.string(), new TypeReference<List<MerchantResponse>>() {});
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                if (response.code() == 422) {
                    throw new UnprocessableEntityError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class));
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            throw new ArgoLabsApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class));
        } catch (IOException e) {
            throw new ArgoLabsException("Network error executing HTTP request", e);
        }
    }

    public MerchantResponse readMerchant(String merchantArgoId) {
        return readMerchant(merchantArgoId, null);
    }

    public MerchantResponse readMerchant(String merchantArgoId, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v1/merchants")
                .addPathSegment(merchantArgoId)
                .build();
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), MerchantResponse.class);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                if (response.code() == 422) {
                    throw new UnprocessableEntityError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class));
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            throw new ArgoLabsApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class));
        } catch (IOException e) {
            throw new ArgoLabsException("Network error executing HTTP request", e);
        }
    }
}
